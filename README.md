# Bước 1: Tạo virtual environment (tùy chọn)
python -m venv venv
venv\Scripts\activate  # Trên Windows

# Bước 2: Cài thư viện
pip install -r requirements.txt

# Bước 3: Chạy script
python watch.py
